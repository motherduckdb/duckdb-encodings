# name: test/sql/encodings.test
# description: test encodings extension
# group: [encodings]

# Before we load the extension, this will fail

require encodings

statement ok
create table T as FROM read_csv('data/test_ascii.csv', encoding = 'ascii')

query I
SELECT COUNT(*) FROM T
----
10